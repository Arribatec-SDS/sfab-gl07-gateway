# sfab-gl07-gateway
sfab‑gl07‑gateway is an integration service for Stena Fastigheter that ingests GL07 XML files, transforms them into Unit4‑compatible transaction objects, and posts them via the Unit4 REST API. The service is designed to be extensible and will support additional source file formats as new systems are onboarded.
